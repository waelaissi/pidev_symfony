<form method="post" action="{{ path('app_sujet_delete', {'idsujet': sujet.idsujet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sujet.idsujet) }}">
    <button class="btn">Delete</button>
</form>
