<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Example 1</title>
    <link rel="stylesheet" href="style.css" media="all" />
    <style>
        .clearfix:after {
            content: "";
            display: table;
            clear: both;
        }

        a {
            color: #5D6975;
            text-decoration: underline;
        }

        body {
            position: relative;
            width: 21cm;
            height: 29.7cm;
            margin: 0 auto;
            color: #2f3459;
            background: #FFFFFF;
            font-family: Arial, sans-serif;
            font-size: 12px;
            font-family: Arial;
        }

        header {
            padding: 20px 0;
            margin-bottom: 30px;
        }

        #logo {
            text-align: center;
            margin-bottom: 10px;
        }

        #logo img {
            width: 90px;
        }

        h1 {
            border-top: 1px solid  #5D6975;
            border-bottom: 1px solid  #5D6975;
            color: #f4b707;
            font-size: 2em;
            line-height: 1.4em;
            font-weight: normal;
            text-align: center;
            margin: 0 0 20px 0;
            padding: 10px;
            background: url('./public/assets/images/logo.png');
        }

        #project {
            float: left;
        }

        #project span {
            color: #5D6975;
            text-align: right;
            width: 52px;
            margin-right: 10px;
            display: inline-block;
            font-size: 0.8em;
        }

        #company {
            float: right;
            text-align: right;
        }

        #project div,
        #company div {
            white-space: nowrap;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            border-spacing: 0;
            margin-bottom: 20px;
        }

        table tr:nth-child(2n-1) td {
            background: #F5F5F5;
        }

        table th,
        table td {
            text-align: center;
        }

        table th {
            padding: 5px 20px;
            color: #f4b707;
            border-bottom: 1px solid #C1CED9;
            white-space: nowrap;
            font-weight: normal;
        }

        table .service,
        table .desc {
            text-align: left;
        }

        table td {
            padding: 20px;
            text-align: right;
            color: #2f3459;
        }

        table td.service,
        table td.desc {
            vertical-align: top;
        }

        table td.unit,
        table td.qty,
        table td.total {
            font-size: 1.2em;
        }

        table td.grand {
            border-top: 1px solid #5D6975;
            font-weight: bold;
        }

        #notices .notice {
            color: #5D6975;
            font-size: 1.2em;
        }
        .bolder{
            font-weight: bolder !important;
            font-size: 19px;
        }

        footer {
            color: #5D6975;
            width: 100%;
            height: 30px;
            position: absolute;
            bottom: 0;
            border-top: 1px solid #C1CED9;
            padding: 8px 0;
            text-align: center;
        }
    </style>
</head>
<body>
<header class="clearfix">
    <h1>Reservation Receipt: #{{ reservation.id }}</h1>
    <div id="company" class="clearfix">
        <div>Tfarhida</div>
        <div>Rue de caire,<br /> Nouvelle medina, TN</div>
        <div>(216) 56353474</div>
        <div><a href="mailto:company@example.com">tfarhida12@gmail.com</a></div>

    </div>
    <div id="project">
        <div><span>CLIENT</span> {{ user.nom~' '~user.prenom }}</div>
        <div><span>ADDRESS</span> {{ user.adresse }}</div>
        <div><span>EMAIL</span> <a href="mailto:john@example.com">{{ user.email }}</a></div>
        <div><span>PHONE</span> {{ user.numTel }}</div>
        <div><span>CREATED AT</span> {{ reservation.idTransaction.createdAt | date("m/d/Y", "Europe/Paris") }}</div>
        <div><span>CHECKIN</span> {{ reservation.dateDebut | date("m/d/Y", "Europe/Paris")  }}</div>
        <div><span>CHECKOUT</span> {{ reservation.dateFin | date("m/d/Y", "Europe/Paris")  }}</div>

    </div>
</header>
<main>
    <table>
        <thead>
        <tr>
            <th class="service">SERVICE</th>
            <th class="desc">DESCRIPTION</th>
            <th>PRICE</th>
            <th>DAYS</th>
            <th>TOTAL</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            {% set diff=date_difference_two(reservation.dateFin | date("Y/m/d"),reservation.dateDebut | date("Y/m/d")) %}
            <td class="service">{{ reservation.type }}</td>
            <td class="desc">{{ reservation.idChambre.idHotel.libelle~' : '~reservation.getIdChambre().type }}</td>
            <td class="unit"> {{ format_to_currency(reservation.idChambre.prix | default(0)) }}</td>
            <td class="qty">{{ diff }}</td>
            <td class="total">{{ format_to_currency(reservation.montantAPayer | default(0)) }}</td>
        </tr>
            <td colspan="4">ADVANCE RATE </td>
            <td class="total">{{ reservation.idTransaction.tauxAvance}}%</td>
        </tr>

        <tr>
            <td colspan="4" class="grand total">PAYED</td>
            <td class="grand total">{{ format_to_currency(reservation.idTransaction.montantPayeAvance | default(0)) }}</td>
        </tr>
        <tr>
            <td colspan="4" class="grand total bolder">REST </td>
            <td class="grand total">{{ format_to_currency(reservation.resteAPayer | default(0)) }}</td>
        </tr>
        </tbody>
    </table>
    <div id="notices">
        <div>NOTICE:</div>
        <div class="notice">This receipt is a proove that you have done a reservation.</div>
    </div>
</main>
<footer>
    Invoice was created on a computer and is valid without the signature and seal.
</footer>
</body>
</html>