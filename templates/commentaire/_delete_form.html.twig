<form method="post" action="{{ path('app_commentaire_delete', {'idsujet':sujet.idsujet,'idcom': commentaire.idcom}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.idcom) }}">
    <button class="btn btn-danger">Delete</button>
</form>
