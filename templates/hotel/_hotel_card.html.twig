<div class="card">
    <img src="{{ vich_uploader_asset(hotel) }}" alt="{{ hotel.libelle }}" class="card-img-top">
    <div class="card-body">
        <div class="card-title text-primary" style="font-weight: bold; font-size: 1.5rem;">{{ hotel.libelle }}</div>
        <div class="text text-warning"></div>
        <p class="text text-muted">{{ hotel.description }}</p>
        <a href="{{ path('app_hotel_show', {id: hotel.id}) }}" class="btn btn-outline-success btn-sm">voir</a>
        <a href="" class="btn btn-outline-danger btn-sm"><i class="fa fa-heart"></i></a>
    </div>
</div>