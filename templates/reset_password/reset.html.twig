{% extends 'base_admin.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}




    <div class="login-page" style="background-image: url(/assetsadmin/images/bg.jpg);">
        <div class="login-from-wrap">
            <div class="login-from">
                <h1 class="site-title">
                    <a href="{{ path('app_hello') }}">
                        <img src="/assetsadmin/images/logo.png" alt="">
                    </a>
                </h1>
                <div class="content">
                    {{ form_start(resetForm) }}
                    {{ form_row(resetForm.plainPassword) }}
                    <button class="btn btn-fill-out btn-block">Reset password</button>
                    {{ form_end(resetForm) }}
                </div>
                <a href="{{ path('app_login') }}" class="for-pass">Login</a>
            </div>
        </div>
    </div>






    {#

    {{ include("partials/title_section.html.twig",{'name':'Rest password'}) }}

<div class="login_register_wrap section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xl-6 col-md-10">
                <div class="login_wrap">
                    <div class="padding_eight_all bg-white">
                        <div class="heading_s1">
                            <h3>Reset Password</h3>
                        </div>
                        <div class="content">
                            {{ form_start(resetForm) }}
                            {{ form_row(resetForm.plainPassword) }}
                            <button class="btn btn-fill-out btn-block">Reset password</button>
                            {{ form_end(resetForm) }}
                        </div>
                        <div class="form-note text-center">Don't Have an Account? <a
                                    href="{{ path('app_register') }}">Sign up now</a></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>#}



{% endblock %}
