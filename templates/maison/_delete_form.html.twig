<form method="post" action="{{ path('app_maison_delete', {'id': maison.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maison.id) }}">
    <button class="btn">Delete</button>
</form>
