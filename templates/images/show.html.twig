{% extends 'base.html.twig' %}

{% block title %}Images{% endblock %}

{% block body %}
    <h1>Images</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ImgId</th>
                <td>{{ image.imgId }}</td>
            </tr>
            <tr>
                <th>ImgBlob</th>
                <td>{{ image.imgBlob }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_images_index') }}">back to list</a>

    <a href="{{ path('app_images_edit', {'imgId': image.imgId}) }}">edit</a>

    {{ include('images/_delete_form.html.twig') }}
{% endblock %}
